source "https://rubygems.org"

# Match GitHub Pages environment locally (Jekyll + approved plugins)
gem "github-pages", group: :jekyll_plugins

# Needed to run `jekyll serve` on newer Ruby
gem "webrick"

title: ValuCompass Support
remote_theme: just-the-docs/just-the-docs
plugins:
  - jekyll-remote-theme

permalink: pretty

defaults:
  - scope:
      path: ""      # everything under /docs (your build root)
      type: "pages"
    values:
      layout: "default"

search_enabled: true

url: https://discovervalucompass.github.io
baseurl: /support-docs
